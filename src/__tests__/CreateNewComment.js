import React from 'react';
import { mount, shallow, render } from 'enzyme';
import CreateNewComment from '../components/CreateNewComment';

test.skip('renders the app', () => {
  render(<CreateNewComment />);
});
