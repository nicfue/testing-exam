import React from 'react';
import { mount, shallow, render } from 'enzyme';
import Comments from '../components/Comments';


beforeEach(() =>{
  localStorage.clear();
});


test.skip('check if there are any comments', () => {

});
